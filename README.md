# TSZ

一个“TypeScript 语法为主、可删减坏特性、追求高性能”的编译型语言与编译器（AOT）。

当前目标（已确认）：

- 方案：A（原生 AOT）
- 平台：全平台导向（macOS/Linux/Windows，先 Host 可用，再补齐）
- 内存：A1（ARC/RC + `Weak`）
- FFI：需要（C ABI）
- `bigint`：重新定义为定长 `i64`（不追求 TS 兼容）
- npm：用于依赖分发/解析；包内源码必须是 TSZ 支持的子集

> 这个仓库处于“从零开始搭骨架”的阶段：先把最小编译链路跑通，再逐步扩展语法、类型系统、优化与工具链。

